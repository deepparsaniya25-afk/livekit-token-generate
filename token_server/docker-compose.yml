services:
  token-server:
    build: .
    ports:
      - "3000:3000"
    env_file:
      - .env
    environment:
      - LIVEKIT_API_KEY=${LIVEKIT_API_KEY}
      - LIVEKIT_API_SECRET=${LIVEKIT_API_SECRET}
    restart: unless-stopped
