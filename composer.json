{
  "name": "livekit-assignment/token-server",
  "description": "PHP token server for LiveKit video call app",
  "type": "project",
  "require": {
    "php": ">=8.1",
    "agence104/livekit-server-sdk": "^1.3",
    "vlucas/phpdotenv": "^5.6"
  },
  "scripts": {
    "start": "php -S localhost:3000 router.php"
  },
  "config": {
    "allow-plugins": {
      "php-http/discovery": true
    }
  }
}
